import React from "react";
import "../styles/SecondaryButton.scss";

const SecondaryButton = ({ label }) => {
    return <button class="SecondaryButton">{label}</button>;
};

export default SecondaryButton;
